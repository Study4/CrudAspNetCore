version: '3'

services:
  ci-build:
    image: microsoft/dotnet:2.1-sdk
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./CrudAspNetCore.sln && dotnet publish ./CrudAspNetCore.sln -c Release -o ./bin/Debug/netcoreapp2.1/Docker/publish"